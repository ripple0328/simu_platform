.row-fluid
  .span12
    .hero-unit
      %legend
        %h1
          .inner-title
            Collection data  trigger Simulation
      %hr/
      %h3
        = link_to "download excel", :action => "download_template" 
      %hr/
      = form_tag("simulation/upload", :multipart => true, :class =>'form-horizontal') do 
        %fieldset
          %h3
            Simulation
          .control-group
            %label.control-label
              Current Load
            .controls
              = render 'shared/status'
          .control-group
            %label.control-label
              Choose a cluster
            .controls
              = radio_button_tag('cluster', 'jm_c', checked = true)
              JM-C cluster
              = radio_button_tag('cluster', 'jm_u', checked = false)
              JM-U cluster
          .control-group
            %label.control-label
              Input Your unique job name
            .controls
              = text_field_tag 'job_name'
          .control-group
            %label.control-label
              Choose file on local disk
            .controls
              = file_field_tag "file"
          .control-group
            %label.control-label
              click wait and be patient!
            .controls
              = submit_tag "Submit file lanuch simulation",:class =>'btn btn-primary'